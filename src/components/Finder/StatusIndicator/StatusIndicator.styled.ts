import { View } from 'react-native'

import styled from 'styled-components/native'

export const StyledStatusIndicator = styled(View)`
	justify-content: center;
	flex-direction: row;
	height: 550px;
`
